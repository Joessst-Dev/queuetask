# LOCAL DEVELOPMENT ONLY — never use these values in production
server:
  port: 8081

db:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: queueti
  sslmode: disable

workflows:
  dir: ./workflows

queueti:
  enabled: false
  grpc_addr: localhost:50051      # queue-ti gRPC server
  admin_url: http://localhost:8080 # queue-ti HTTP admin API (used for auth)
  username: ""
  password: ""
